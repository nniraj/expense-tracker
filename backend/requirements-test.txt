# Testing dependencies for Expense Tracker Backend
# Install with: pip install -r requirements-test.txt

# Core testing framework
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-xdist>=3.3.1

# Additional testing utilities
pytest-flask>=1.2.0
pytest-env>=0.8.2
pytest-timeout>=2.1.0

# Code quality and coverage
coverage>=7.2.0

# All dependencies from requirements.txt are also needed
# (pytest will need them to run the application)
